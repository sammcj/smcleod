---
title: "Page scripts"
description: "A page that carries its own scripts, which run whether it loads directly or opens in a window."
icon: tools
---

Pages can include their own scripts through a shortcode or a layout. The shell runs them in order when the page opens in a window, waiting for external scripts to load first.

{{< counter >}}
